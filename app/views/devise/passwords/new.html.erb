<!DOCTYPE html>
<nav class="navbar navbar-expand-lg navbar-light bg-danger">
  <div class="container">
    <%= link_to ".LIFT()", root_path, class: "navbar-brand" %>

      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <%= link_to current_user.email, edit_user_registration_path, class: "nav-item nav-link" %>
          <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-item nav-link" %>
        <% else %>
          <%= link_to "Home", root_path, class: "navbar-brand" %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


<html lang="en" dir="ltr">
  <head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
  </head>

  <body class="form-body">
<div class="password">
<div class="white-box2">
<h2>Forgot your password?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "Send reset instructions" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>

</div>
      <footer>
          <a href='https://github.com/orgs/JJLC-Corporation/repositories'target="_blank" class="devise-footer">
            © 2022 JJLC Corporation
          </a>
      </footer>      
  </body>
</html>